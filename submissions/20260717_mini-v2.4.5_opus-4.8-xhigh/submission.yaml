# Generated by `programbench submit package` from: /home/john/pb_submissions/20260717_mini-v2.4.5_opus-4.8-xhigh
# [auto] fields are recomputed on every `programbench submit package`; all other fields are preserved.
schema_version: 1

submission_id: "20260717_mini-v2.4.5_opus-4.8-xhigh"
submitter:
  name: "ProgramBench"
  contact: "@programbench"    # email or @github
  affiliation: "Meta"

system:
  agent: "mini-SWE-agent"                   # scaffold/harness; "none" for a pure human submission
  description_url: "README.md"
  is_os_model: false       # true if the model's weights are openly available
  is_os_scaffold: true # true if the agent/scaffold is open source
  model: "Claude Opus 4.8 (xhigh reasoning)"                   # display name used on the leaderboard
  provider: "Anthropic"
  type: "single-agent"              # single-agent | multi-agent | other

eval:
  programbench_version: "1.2.4"   # [auto]
# Scores are not stored here: the leaderboard recomputes them from _stats/score.json with the
# registry's current ignored-tests list, so any cached numbers would just go stale.
