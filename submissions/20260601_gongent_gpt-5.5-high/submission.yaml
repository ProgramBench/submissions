# Generated by `programbench submit package` from: 20260601_gongent_gpt-5.5-high
# [auto] fields are recomputed on every `programbench submit package`; all other fields are preserved.
schema_version: 1

submission_id: "20260601_gongent_gpt-5.5-high"
submitter:
  name: "Qucheng Gong"
  contact: "@qucheng-gong"    # email or @github
  affiliation: ""

system:
  agent: "Gongent"                   # scaffold/harness; "none" for a pure human submission
  description_url: "https://github.com/qucheng-gong/gongent"
  is_os_model: false       # true if the model's weights are openly available
  is_os_scaffold: true # true if the agent/scaffold is open source
  model: "GPT-5.5 (high)"                   # display name used on the leaderboard
  provider: "OpenAI"
  type: "multi-agent"              # single-agent | multi-agent | other

eval:
  programbench_version: "1.2.2"   # [auto]
# Scores are not stored here: the leaderboard recomputes them from _stats/score.json with the
# registry's current ignored-tests list, so any cached numbers would just go stale.
