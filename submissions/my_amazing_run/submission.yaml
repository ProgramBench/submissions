# Generated by `programbench submit package` from: my_amazing_run
# [auto] fields are recomputed on every `programbench submit package`; all other fields are preserved.
schema_version: 1

submission_id: "my_amazing_run"
submitter:
  name: "Bruno Loureiro"
  contact: "@Ahavahdev1"    # Seu usuário do GitHub associado ao PR
  affiliation: "Independent"

system:
  agent: "MEA v5.8.1 (AGIR)"                   # Nome do seu framework/scaffold
  description_url: "README.md"
  is_os_model: true       # true porque os pesos do Llama 3.1 são de código aberto
  is_os_scaffold: false   # false para manter as regras de escrita e decisão do MEA fechadas
  model: "Llama-3.1-8B"   # Nome de exibição do modelo na classificação oficial
  provider: "Meta"
  type: "multi-agent"     # MEA opera sob arquitetura de multi-agentes (SRE, Logos, Guardian)

eval:
  programbench_version: "1.2.4"   # [auto]
# Scores are not stored here: the leaderboard recomputes them from _stats/score.json with the
# registry's current ignored-tests list, so any cached numbers would just go stale.